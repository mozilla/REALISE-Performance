## Context

This GitHub repository is part of the work done to investigate the usage of change point detection (CPD) methods to ctch performance anomalies in Mozilla workflow. The data associated with this project can be found [here](https://doi.org/10.5281/zenodo.20382322).

## Repository breakdown

Currently, the work done has revolved around extracting data from Mozilla system, collect annotation data from Mozilla experts, and try different change point detection techniques to detect anomalies in Mozilla data. The work that has been done exists in the folders `data_annotation_platform`, `data_extraction_transformation`, `prediction_generation`, and `prediction_postprocessing_scripts`.

Every subfolder has its own `README.md` file with a breakdown of its content. Here is a quick recap of the content of erach folder.

## data_annotation_platform overview

The `data_annotation_platform` folder contains the code of the data annotations platform used to collect ground truth to assess the CPD methods.

## data_extraction_transformation overview

The `data_extraction_transformation` folder contains scripts and notebooks to extract performance-related data from [Treeherder API](https://treeherder.mozilla.org/docs/), tranform them, and do preliminary analysis on them.

## prediction_generation overview

The `prediction_generation` folder contains scripts to detect the change points in the data using [TCPDBench](https://github.com/SimonEismann/TCPDBench). The TCPDBench project is extended and altered to fit the use case of this project by adding several CPD mrthods' implementations.

## prediction_postprocessing_scripts overview

The `prediction_postprocessing_scripts` folder contains scripts to further tranform and analyze the data generated by TCPDBench by combining the results of several CPD methods for example. The code related to the voting strategies used in this project are in this folder.

## License
The code in this repository is licensed under the MIT license, unless 
otherwise specified. See the [LICENSE file](LICENSE) for further details. 
Reuse of the code in this repository is allowed. Make sure to check [this](https://arxiv.org/abs/2003.06222) publication as its replication package was used to do the annotation data collection (check `data_annotation_platform` folder) and to run CPD methods (check `prediction_generation` folder).